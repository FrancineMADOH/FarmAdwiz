import 'package:flutter/material.dart';

class UsingConditions extends StatelessWidget {
  const UsingConditions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    //TODO : ask for terms of use
    return Container();
  }
}
